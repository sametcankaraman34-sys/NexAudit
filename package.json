{
  "name": "nexaudit",
  "version": "1.0.0",
  "private": true,
  "description": "NexAudit monorepo — deploy hedefi frontend/",
  "scripts": {
    "vercel-build": "cd frontend && npm install && npm run build",
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm run dev"
  },
  "engines": {
    "node": ">=20"
  }
}
